@using EpiBookingSystem
@using EPiServer.Core
@using EPiServer.Web.Mvc.Html
@model RegisterViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="authenticate-container">
    <div class="pure-u">
        <h2 class="brand-tagline">Skapa konto</h2>
        @using (Html.BeginForm("Register", "Account", FormMethod.Post, new { @class = "pure-form align-center" }))
        {

            <fieldset class="pure-group">
                @Html.TextBoxFor(m => m.Username, new { placeholder = "Användarnamn", @class = "pure-input-1-1" })
                @Html.TextBoxFor(m => m.Email, new { placeholder = "Mailadress", @class = "pure-input-1-1" })
                @Html.PasswordFor(m => m.Password, new { placeholder = "Lösenord", @class = "pure-input-1-1" })
                @Html.PasswordFor(m => m.ConfirmPassword, new { placeholder = "Bekräfta lösenord", @class = "pure-input-1-1" })

                @Html.ValidationSummary(false, "", new { @class = "pure-form-message-inline" })
                <div class="pure-control-group">

                </div>
                <div class="pure-control-group">
                    <input type="submit" class="pure-button pure-button-primary authenticate-button" value="Skapa konto" />
                </div>

            </fieldset>

        }



    </div>
    </div>